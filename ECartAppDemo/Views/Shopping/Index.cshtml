@model IEnumerable<ECartAppDemo.ViewModel.ShoppingViewModel>
@{
    ViewBag.Title = "Index";
}


<div class="container">
    <h2 class="text-center">Product List</h2>
    @foreach (var item in Model)
    {
        <div class="col-md-4" style="border:2px solid black">
            <div style="text-align:center; border-bottom: 1px solid maroon">
                <h4>@item.ItemName</h4>
            </div>
            <div>
                <div class="col-md-8">
                    <img class="img-responsive" src="@Url.Content(item.ImagePath)" height="150" weigh="150" />
                </div>
                <div class="col-md-4" style="text-align:left">
                    <b>@item.Description</b>
                </div>
            </div>


            <div class="col-md-8" style="font-size:18px; border-top: 2px solid maroon; padding:2px">
                <b>@item.ItemPrice.ToString("c")</b>

            </div>
            <div class="col-md-4" style="text-align: right; margin-bottom:10px; border-top: 2px solid maroon; padding:2px">
                <input type="button" name="AddToCart" value="Add to Cart" class="btn btn-primary" itemid="@item.ItemId" id="btnAddToCart" onclick="AddToCart()" />
            </div>
        </div>
    }
</div>

